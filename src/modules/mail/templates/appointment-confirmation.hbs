<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; line-height: 1.6; color: #333; margin: 0; padding: 0; background-color: #f4f7f9; }
        .container { max-width: 600px; margin: 20px auto; background: #ffffff; border-radius: 8px; overflow: hidden; box-shadow: 0 4px 10px rgba(0,0,0,0.05); }
        .header { background-color: #004a99; color: #ffffff; padding: 30px; text-align: center; }
        .content { padding: 30px; }
        .footer { background-color: #f8f9fa; color: #6c757d; padding: 20px; text-align: center; font-size: 12px; }
        .card { border: 1px solid #e1e4e8; border-radius: 6px; padding: 20px; margin-top: 20px; background-color: #fafbfc; }
        .btn { display: inline-block; padding: 12px 24px; background-color: #007bff; color: #ffffff; text-decoration: none; border-radius: 4px; margin-top: 20px; font-weight: bold; }
        .label { font-weight: bold; color: #004a99; width: 120px; display: inline-block; }
        .value { margin-bottom: 8px; }
        h1 { margin: 0; font-size: 24px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Seguridad Integral</h1>
            <p>Confirmación de Cita</p>
        </div>
        <div class="content">
            <p>Hola <strong>{{customerName}}</strong>,</p>
            <p>Tu cita ha sido programada con éxito. Aquí tienes los detalles:</p>
            
            <div class="card">
                <div class="value"><span class="label">ID de Cita:</span> {{appointmentId}}</div>
                <div class="value"><span class="label">Fecha y Hora:</span> {{dateTimeFormatted}}</div>
                <div class="value"><span class="label">Modalidad:</span> {{modality}}</div>
                {{#if address}}
                <div class="value"><span class="label">Dirección:</span> {{address}}</div>
                {{/if}}
                {{#if notes}}
                <div class="value"><span class="label">Notas:</span> {{notes}}</div>
                {{/if}}
            </div>

            <p>Si necesitas realizar algún cambio, por favor contáctanos lo antes posible.</p>
            
            <a href="{{publicUrl}}" class="btn">Ver Detalles</a>
        </div>
        <div class="footer">
            &copy; 2025 Seguridad Integral - Todos los derechos reservados.<br>
            Este es un correo automático, por favor no respondas.
        </div>
    </div>
</body>
</html>
